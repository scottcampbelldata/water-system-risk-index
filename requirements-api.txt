# Runtime dependencies for the FastAPI backend (waterapi/).
# The data pipeline's dependencies live in requirements.txt.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
httpx>=0.27   # required by fastapi/starlette TestClient (API smoke test)
