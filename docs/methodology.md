# Methodology

See the final methodology artifact at `methodology_notes.md` in the project root.

This project uses a transparent weighted scoring model, not a predictive model. It prioritizes review and does not claim that any system is unsafe or legally noncompliant beyond what source records explicitly contain.
