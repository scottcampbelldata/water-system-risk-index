# Developer / CI tooling: linting, type-checking, test coverage, dependency audit.
# Install with: python -m pip install -r requirements-dev.txt
# (or `pip install -e .[dev]`). Runtime deps live in requirements.txt /
# requirements-api.txt.
ruff>=0.6
mypy>=1.11
pytest>=8.0
pytest-cov>=5.0
pip-audit>=2.7
httpx>=0.27
pandas-stubs
types-PyYAML
types-requests
